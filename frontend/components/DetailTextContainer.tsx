type DetailTextContainerProps = {
	date: string;
	client?: string;
	paragraphs: string[];
};

export const DetailTextContainer = ({
	date,
	client,
	paragraphs,
}: DetailTextContainerProps) => {
	return <div></div>;
};
